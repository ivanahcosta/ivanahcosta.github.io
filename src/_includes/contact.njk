{% if contact %}
  <section class="gap-0" id="ribbons">
    {% for i in range(0, 2) %}
      <div class="bg-[#959595] text-[#fb3f1a] text-[1.5em] uppercase relative overflow-x-hidden flex justify-center items-center {{ '!bg-[#fb3f1a] !text-[#d6d6d6]  rotate-3 -translate-y-1/2' if loop.last }}" data-marquee>
        <div class="py-10 w-[200vw] flex gap-5 items-center justify-center">
          <p class="shrink-0">Available for work</p>
        </div>
      </div>
    {% endfor %}
  </section>

  <section id="contact">
    <div class="container" style="container-type: inline-size;">
      <h2 class="-ml-[1cqw] mt-[2cqw] uppercase text-[26cqw]">
        <div class="-mt-[11cqw] flex items-center justify-between">
          <span>Hey,</span>
          <span>get</span>
          <span>in</span>
        </div>
        <div class="-mt-[11cqw] flex items-center justify-between">
          <span>touch</span>
          <span class="emoji">📬</span>
        </div>
        <div class="-mt-[11cqw] flex items-center justify-between">
          <span>✨</span>
          <span>
            <a class="underline" href="mailto:ivanahcosta@gmail.com" target="_blank" data-cursor-text="✉">:::mail</a>
          </span>
          <span>or</span>
        </div>
      </h2>
    </div>
    <div class="container flex justify-between" style="container-type: inline-size;">
      {% for item in socialLinks %}
        <div class="border border-current rounded-full text-[7cqw]">
          <a class="underline inline-flex gap-3 items-center" href="{{ item.link }}" target="_blank" data-cursor-text="↗">
            <span>{{ item.text }}</span>
          </a>
        </div>
      {% endfor %}
    </div>
  </section>
{% endif %}